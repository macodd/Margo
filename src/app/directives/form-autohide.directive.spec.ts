import { FormAutohideDirective } from './form-autohide.directive';

describe('FormAutohideDirective', () => {
  it('should create an instance', () => {
    const directive = new FormAutohideDirective();
    expect(directive).toBeTruthy();
  });
});
